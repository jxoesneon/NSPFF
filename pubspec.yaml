name: nspff
description: "NSPFF (NSP Fast Forward) - A Flutter Android application for forwarding Nintendo Switch NRO apps and RetroArch ROMs."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  crypto: ^3.0.3
  image: ^4.1.7
  path: ^1.9.0
  shared_preferences: ^2.2.2
  file_picker: ^8.3.7
  path_provider: ^2.1.2
  google_fonts: ^8.2.1
  http: ^1.2.0
  iconic_morph: ^1.9.0
  qr_flutter: ^4.1.0
  flutter_secure_storage: ^10.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
